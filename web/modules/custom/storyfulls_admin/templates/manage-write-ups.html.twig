<div class="manage-page">
  <h1 class="page-title">Manage Write Ups</h1>
  
  <div class="manage-header">
    <a href="/node/add/write_up" class="btn btn-create">Create Write Up</a>
  </div>

  <div class="filter-section">
    <form method="get" action="/admin/dashboard/manage-write-ups" class="filter-form">
      <div class="filter-group">
        <label for="status">Published status <span class="required">*</span></label>
        <select name="status" id="status" class="form-select">
          <option value="">All</option>
          <option value="yes" {% if current_status == 'yes' %}selected{% endif %}>Yes</option>
          <option value="no" {% if current_status == 'no' %}selected{% endif %}>No</option>
        </select>
      </div>

      <div class="filter-group">
        <label for="name">Name</label>
        <input type="text" name="name" id="name" value="{{ current_name }}" class="form-input" />
      </div>

      <button type="submit" class="btn btn-apply">Apply</button>
    </form>
  </div>

  <div class="data-table">
    <table>
      <thead>
        <tr>
          <th>Sl No</th>
          <th>Name</th>
          <th>Edit</th>
          <th>Delete</th>
        </tr>
      </thead>
      <tbody>
        {% if writeups|length > 0 %}
          {% for writeup in writeups %}
            <tr>
              <td>{{ writeup.sl_no }}</td>
              <td>{{ writeup.name }}</td>
              <td><a href="/node/{{ writeup.nid }}/edit" class="action-link edit-link">edit</a></td>
              <td><a href="/admin/dashboard/delete-book/{{ writeup.nid }}" class="action-link delete-link" onclick="return confirm('Are you sure you want to delete this write up?');">delete</a></td>
            </tr>
          {% endfor %}
        {% else %}
          <tr>
            <td colspan="4" class="no-results">No write ups found.</td>
          </tr>
        {% endif %}
      </tbody>
    </table>
  </div>
</div>
