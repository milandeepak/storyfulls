<div class="manage-page">
  <h1 class="page-title">Manage Events</h1>
  
  <div class="manage-header">
    <a href="/node/add/event" class="btn btn-create">Create Event</a>
  </div>

  <div class="filter-section">
    <form method="get" action="/admin/dashboard/manage-events" class="filter-form">
      <div class="filter-group">
        <label for="status">Published status <span class="required">*</span></label>
        <select name="status" id="status" class="form-select">
          <option value="">All</option>
          <option value="yes" {% if current_status == 'yes' %}selected{% endif %}>Yes</option>
          <option value="no" {% if current_status == 'no' %}selected{% endif %}>No</option>
        </select>
      </div>

      <div class="filter-group">
        <label for="name">Name</label>
        <input type="text" name="name" id="name" value="{{ current_name }}" class="form-input" />
      </div>

      <button type="submit" class="btn btn-apply">Apply</button>
    </form>
  </div>

  <div class="data-table">
    <table>
      <thead>
        <tr>
          <th>Sl No</th>
          <th>Name</th>
          <th>Edit</th>
          <th>Delete</th>
        </tr>
      </thead>
      <tbody>
        {% if events|length > 0 %}
          {% for event in events %}
            <tr>
              <td>{{ event.sl_no }}</td>
              <td>{{ event.name }}</td>
              <td><a href="/node/{{ event.nid }}/edit" class="action-link edit-link">edit</a></td>
              <td><a href="/admin/dashboard/delete-book/{{ event.nid }}" class="action-link delete-link" onclick="return confirm('Are you sure you want to delete this event?');">delete</a></td>
            </tr>
          {% endfor %}
        {% else %}
          <tr>
            <td colspan="4" class="no-results">No events found.</td>
          </tr>
        {% endif %}
      </tbody>
    </table>
  </div>
</div>
